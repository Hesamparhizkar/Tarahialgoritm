python
word = input("Enter a word: ")

new_word = ""
for char in word:
    if char.isupper():
        new_word += char.lower()
    else:
        new_word += char.upper()

print(f"The new word is: {new_word}")
